services:
  app:
    build: .
    container_name: aichat-app
    restart: always
    ports:
      - '8010:8000'
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    extra_hosts:
      - 'host.docker.internal:host-gateway'
